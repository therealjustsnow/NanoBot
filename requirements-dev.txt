# NanoBot dev/test dependencies
# Install with: pip install -r requirements-dev.txt
# Requires requirements.txt to already be installed (discord.py is needed by utils/helpers.py)

pytest>=9.0.3
pytest-asyncio>=0.23.0
