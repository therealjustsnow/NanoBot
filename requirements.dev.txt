# NanoBot dev/test dependencies
# Install with: pip install -r requirements.dev.txt
# This is a superset of requirements.txt — it pulls in all runtime deps plus the
# dev/test extras below, so a single install gets you a runnable + testable env.

-r requirements.txt

pytest>=9.1.1
pytest-asyncio>=1.4.0
dpytest>=0.7.0

# Exercises the encrypted-DB path in tests/test_db_crypto.py (optional at
# runtime — see the commented line in requirements.txt)
sqlcipher3>=0.6.2

# Linting / formatting (also wired into .pre-commit-config.yaml)
black>=26.5.1
ruff>=0.15.22
pre-commit>=4.6.1
