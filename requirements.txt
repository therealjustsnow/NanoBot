# NanoBot dependencies
# Install with: pip install -r requirements.txt

discord.py>=2.7.1
aiosqlite>=0.22.1
aiohttp>=3.14.1
psutil>=7.2.2
Pillow>=12.2.0

# Music player (cogs/music.py) — also requires the FFmpeg binary on PATH
PyNaCl>=1.6.2
yt-dlp[default]>=2026.7.4
# yt-dlp plugin: fetches YouTube GVS PO tokens to avoid "format not available" /
# HTTP 403 on gated formats. Needs a running provider server (Docker, or Node/Deno
# via `music_pot_provider_url`) — see example_config.ini + the Configuration wiki.
bgutil-ytdlp-pot-provider>=1.3.1

# Optional: database encryption at rest (only needed when db_encryption_key /
# NANOBOT_DB_KEY is set — see example_config.ini)
# sqlcipher3-binary>=0.6.0
